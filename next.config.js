/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router enabled by default in `app/`
  // Export static HTML for GitHub Pages
  output: 'export',
  // For a user/organization site (saitenntaisei.github.io), no basePath is needed
};

module.exports = nextConfig;

